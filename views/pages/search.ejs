<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>고객정보관리</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <div class="container-sm">
      <h1 class="my-3">
        고객정보관리 <a href="/" class="btn btn-success">HOME</a>
      </h1>
      <form
        id="search"
        class="input-group mb-3 d-flex align-items-center"
        action="/search"
        method="post"
      >
        <input
          type="text"
          name="searchdata"
          id="searchdata"
          autocomplete="off"
          placeholder="고객명으로 검색"
          class="form-control"
        />
        <button class="btn btn-primary" type="submit">검색</button>
      </form>
      <span class="mt-2">검색어 : "<%= searData %>"</span>
      <table class="table table-hover align-middle">
        <th>id</th>
        <th>name</th>
        <th>age</th>
        <th>sex</th>
        <th>contact</th>
        <th></th>
        <% for( let i = 0; i < userInfo.length; i++ ) { %>
        <tr>
          <td><%= userInfo[i].id %></td>
          <td><%= userInfo[i].username %></td>
          <td><%= userInfo[i].age %></td>
          <td><%= userInfo[i].sex %></td>
          <td><%= userInfo[i].contact %></td>
          <td>
            <form
              action="/delete/<%= userInfo[i].id %>"
              method="post"
              onsubmit="let result = confirm('삭제하시겠습니까?'); if(!result) return false;"
            >
              <button class="btn btn-danger" type="submit">삭제</button>
            </form>
          </td>
        </tr>
        <% } %>
      </table>
      <span class="mt-3">Total Posts : <%= userInfo.length %></span>
      <h1 class="my-3">정보입력</h1>
      <form action="/create" method="post" class="info">
        <div class="username my-3">
          <input
            class="p-2 form-control"
            type="text"
            name="username"
            id="username"
            placeholder="이름 입력"
            required
            autocomplete="off"
          />
        </div>
        <div class="age my-3">
          <input
            class="p-2 form-control"
            type="text"
            name="age"
            id="age"
            placeholder="나이 입력"
            autocomplete="off"
            required
          />
        </div>
        <div class="gender my-3">
          <label
            >남성<input
              class="ms-3"
              type="radio"
              name="sex"
              id="sex"
              value="남성"
              checked /></label
          ><br />
          <label
            >여성<input
              class="ms-3"
              type="radio"
              name="sex"
              id="sex"
              value="여성"
          /></label>
        </div>
        <div class="contact my-3">
          <input
            class="p-2 form-control"
            type="text"
            name="contact"
            id="contact"
            placeholder="연락처( ' - ' 을 포함해서 입력)"
            autocomplete="off"
          />
        </div>
        <div class="addbtn my-3">
          <button class="btn btn-primary" type="submit">추가</button>
        </div>
      </form>
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
